import React from 'react';
import List from './list'

class ListsIndex
 extends React.Component {
    render() {
        return(
            <div className='lists-index'>

                {/* {this.props.lists.slice(0,numItemsShown).map((i) => (
                    <List/>
                ))} */}
                
            </div>
        )
    }
}

export default ListsIndex

